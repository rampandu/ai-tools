# Next.js AI Dev Tools (Regex & SQL)

1. Copy files into a folder.
2. Create `.env.local` from `.env.example` and set HF_TOKEN and GOOGLE_ADSENSE_ID.
3. Install dependencies: `npm install`.
4. Run locally: `npm run dev`.
